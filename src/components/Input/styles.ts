import styled from "styled-components";

const InputContainer = styled.div`
    input {
        color: black;
        height: 2rem;
        font-size: 0.75rem;
        font-weight: 600;
    }

    p {
        color: #CECECE;
    }

    span {
        color: red;
    }
`

export default InputContainer
