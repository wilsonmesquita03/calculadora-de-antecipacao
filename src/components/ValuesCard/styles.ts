import styled from "styled-components";

const IValuesCardContainer = styled.div`
    display: flex;
    width: 100%;
    justify-content: space-between;

    span {
        font-size: 0.9rem;
        font-weight: bold;
        color: var(--grey-0);
    }

    p {
        font-size: 1rem;
        font-weight: 400;
        color: var(--grey-0);
    }

`

export { IValuesCardContainer }